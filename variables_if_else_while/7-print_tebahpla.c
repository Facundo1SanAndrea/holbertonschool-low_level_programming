#include<stdio.h>

/**
 * main - prints alphabet revers
 * Return: zerp
 */

int main(void)
{
	char alph;

	for (alph = 'z'; alph <= 'a'; alph++)
	{
		putchar(alph);
	}
	putchar('\n');

	return (0);

}
